#ifndef NO_STREAM

#define Uses_TDirCollection
#define Uses_TStreamableClass
#include <tv.h>

TStreamableClass TV_EXPORT RDirCollection( TDirCollection::name,
                                 TDirCollection::build,
                                 __DELTA(TDirCollection)
                               );

#endif

